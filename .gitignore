# 融合脚本必须入仓：GitHub Actions 云端重建站点时要调用
__pycache__/
*.pyc

# 系统文件
.DS_Store

# 桌面部署包不应出现在仓库
*.zip

# 云端运行时产物与字体（不入仓，Actions 每次现下载）
out/
.fonts/

# 旧的「每30分钟云端融合」方案：与本地自动化重复，且 Netlify 已有云端构建命令，暂不启用。
# 如需启用，删除下面这一行并推送即可。
.github/workflows/site-sync.yml
