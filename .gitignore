# 融合脚本必须入仓：GitHub Actions 云端重建站点时要调用
__pycache__/
*.pyc

# 系统文件
.DS_Store

# 桌面部署包不应出现在仓库
*.zip

# 云端运行时产物与字体（不入仓，Actions 每次现下载）
out/
.fonts/

# 待 PAT 补 workflow 权限后再入仓（当前推送会被 GitHub 拒绝，故先本地保留）
.github/workflows/
